public static int passingCars(int[] a) {
    int countZero = 0, passing = 0;
    for (int i = 0; i < a.length; i++) {
        if (a[i] == 1) {
            passing += countZero;
        } else countEast++;
    }
    return passing;
}
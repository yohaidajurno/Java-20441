public static int countSubArrays(int[] arr, int num) {
    int count = 0;
    int sum = 0;

    for (int i = 0; i < arr.length; i++) {
        if (arr[i] <= num) {
            count++;
            sum += count;
        } else {
            count = 0;
        }
    }

    return sum;
}